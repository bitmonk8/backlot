//! mech stage — placeholder. Real tests land in a later deliverable.

use crate::runner::StageContext;
use crate::types::TestResult;

pub fn run(_ctx: &StageContext) -> Vec<TestResult> {
    Vec::new()
}
